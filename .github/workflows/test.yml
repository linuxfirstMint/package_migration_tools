name: Test

on:
  push:
    paths:
      - "**.py"
      - "tests/**"
      - ".github/workflows/test.yml"
  pull_request:
  workflow_dispatch:

jobs:
  python-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.13"

      - name: Install uv
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          uv v

      - name: Install dependencies
        run: uv pip install -r requirements.txt

      - name: Run pytest
        run: uv run pytest

  # bats-tests:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: TODO: Setup bats-core and run Bats tests
  #       run: echo "Bats tests will be added after environment is ready (see issue #5)"
